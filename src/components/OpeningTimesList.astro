---
import { openingTimes } from '../data/site';

interface Props {
  headingClass?: string;
}

const { headingClass = 'text-lotg-green' } = Astro.props;
---

<div class="bg-lotg-card rounded-2xl p-6 border border-lotg-green/20">
  <h3 class={`font-bangers text-3xl tracking-wide mb-5 ${headingClass}`}>Opening Times</h3>
  <div class="space-y-3">
    {openingTimes.map(row => (
      <div class="border-b border-lotg-green/10 pb-3 last:border-0 last:pb-0">
        <div class="flex justify-between gap-4 items-start">
          <span class="text-lotg-offwhite/70 text-sm uppercase tracking-wide shrink-0">{row.label}</span>
          <div class="text-lotg-green text-sm text-right">
            {row.times.map(line => (
              <div>{line}</div>
            ))}
          </div>
        </div>
        {row.note && (
          <p class="mt-1 text-lotg-offwhite/50 text-xs leading-relaxed text-right">{row.note}</p>
        )}
      </div>
    ))}
  </div>
</div>
